extends layouts/default

block content
	main.grid
		section.grid__item-sm
			h1= title
			h2= description

			p
				each tag in tags
					a.tag(href=tag[1], target='_blank')= `#${tag[0]}`

			p #[+icon('github')] #[small See #[u #[a(href="https://github.com/mantis-stack/mantis-starter", target="_blank") Mantis Starter on GitHub]]].
